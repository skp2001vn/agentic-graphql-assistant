/**
 * Immutable inputs and outputs exchanged through the bounded GraphQL tool surface.
 *
 * <p>Tool implementations remain in the parent package so {@code GraphqlAssistantTools} and {@code
 * SchemaInspectionTool} are the first implementation classes contributors encounter.
 */
package com.example.graphqlassistant.tools.model;
