/**
 * Immutable HTTP response and error contracts exposed by the assistant API.
 *
 * <p>Controllers and exception translation remain in the parent package so the primary HTTP entry
 * points are immediately visible.
 */
package com.example.graphqlassistant.api.model;
